<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MHealth"
        tools:targetApi="31">

        <!--
         CORRECTION : SplashActivity est maintenant le point de lancement.
         C'est le premier écran que l'utilisateur verra.
        -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.MHealth"> <!-- Thème optionnel pour plein écran -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
         LoginActivity n'est plus le point de lancement.
         Elle sera appelée par SplashActivity.
        -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- Déclaration des autres activités -->
        <activity android:name=".SignUpActivity" android:exported="false" />
        <activity android:name=".WaitingForConfirmationActivity" android:exported="false" />
        <activity android:name=".PatientDashboardActivity" android:exported="false" />

    </application>

</manifest>
