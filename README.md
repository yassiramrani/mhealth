# mhealth

Android health application project managed with Gradle.

## Development

Open the project in Android Studio, or build it with the Gradle wrapper:

```bash
./gradlew build
```

On Windows, use `gradlew.bat build`.
